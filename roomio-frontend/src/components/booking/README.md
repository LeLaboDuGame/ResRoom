# Booking

- `BookingForm.jsx` — Form for creating reservations with date, time, duration, title, and name fields, including overlap validation.
- `BookingButton.jsx` — Reusable booking trigger with "plus" circle or default accent button variants.
- `DeleteModal.jsx` — Accessible confirmation dialog requiring "oui" typed in to confirm reservation deletion.
